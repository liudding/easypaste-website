# 2026-07-24

## SEO 双语优化（会话工作）
- **诊断关键瓶颈**：原英文版为 JS 同-URL 切换、无 hreflang → 百度不可见英文，Google 无语言信号。这是「兼顾中英文」的最大 blocker。
- **已实施修复**：
  - 新建服务端渲染英文页 `en/index.html`、`en/blog/index.html`、`en/blog/mac-clipboard-manager-guide.html`，各带英文 JSON-LD（SoftwareApplication/Organization/FAQPage + Article/BreadcrumbList）。
  - 全站互设 `hreflang="zh-CN"/"en"/"x-default"`（中文首页+博客页、英文页均加）。
  - 中文页 EN 按钮改为跳转 `/en/`（去掉 JS 渲染英文的依赖）。
  - 中文首页加 GSC/百度/Bing 验证 meta 占位。
  - 生成 `og-image.png`（1200×630，Pillow 生成，脚本已删）。
  - `sitemap.xml` 扩为 6 条双语 URL + hreflang。
  - 新建 `SEO-STRATEGY-BILINGUAL.md`（中英双语总纲），更新 `overview.md`。
- **待用户**：自定义域名（建议 easypaste.app）、填验证 ID 并提交 sitemap、接 GA4、实测 CWV、按策略扩内容集群。
- **环境备注**：图片生成用 `/Users/ding/.workbuddy/binaries/python/envs/default` 的 Pillow（venv 已建）。
